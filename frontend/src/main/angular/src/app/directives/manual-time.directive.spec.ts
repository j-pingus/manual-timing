import { ManualTimeDirective } from './manual-time.directive';

describe('ManualTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new ManualTimeDirective();
    expect(directive).toBeTruthy();
  });
});
